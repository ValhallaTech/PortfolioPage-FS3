name: Yarn Config

on: [push]

jobs:
  yarn-config:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Run yarn config
        run: yarn config
